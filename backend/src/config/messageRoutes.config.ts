const routes = {
  unreadCount: '/api/messages/unread-count',
  markRead: '/api/messages/mark-read',
  thread: '/api/messages/:bookingId',
  reply: '/api/messages/:bookingId/reply',
}

export default routes
