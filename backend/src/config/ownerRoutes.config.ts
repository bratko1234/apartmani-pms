const routes = {
  dashboard: '/api/owner/dashboard',
  revenue: '/api/owner/revenue/:year/:month',
  calendar: '/api/owner/calendar/:propertyId/:year/:month',
  occupancyTrend: '/api/owner/occupancy-trend',
  revenueTrend: '/api/owner/revenue-trend',
}

export default routes
